name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
            bin_ext: ""

          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            archive: tar.gz
            bin_ext: ""

          - os: macos-latest
            target: x86_64-apple-darwin
            archive: tar.gz
            bin_ext: ""

          - os: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz
            bin_ext: ""

          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip
            bin_ext: ".exe"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - name: Install Linux ARM64 cross linker
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Configure Linux ARM64 linker
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          mkdir -p .cargo
          cat >> .cargo/config.toml <<'EOF'
          [target.aarch64-unknown-linux-gnu]
          linker = "aarch64-linux-gnu-gcc"
          EOF

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package Unix
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          set -euxo pipefail

          VERSION="${GITHUB_REF_NAME}"
          NAME="split-${VERSION}-${{ matrix.target }}"
          mkdir -p "dist/${NAME}"

          cp "target/${{ matrix.target }}/release/split" "dist/${NAME}/split"
          cp README.md "dist/${NAME}/" || true
          cp LICENSE "dist/${NAME}/" || true

          cd dist
          tar -czf "${NAME}.tar.gz" "${NAME}"

      - name: Package Windows
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $Version = "${env:GITHUB_REF_NAME}"
          $Name = "split-$Version-${{ matrix.target }}"
          New-Item -ItemType Directory -Force -Path "dist/$Name"

          Copy-Item "target/${{ matrix.target }}/release/split.exe" "dist/$Name/split.exe"
          if (Test-Path "README.md") { Copy-Item "README.md" "dist/$Name/" }
          if (Test-Path "LICENSE") { Copy-Item "LICENSE" "dist/$Name/" }

          Compress-Archive -Path "dist/$Name" -DestinationPath "dist/$Name.zip"

      - name: Upload release assets
        uses: softprops/action-gh-release@v3
        with:
          files: |
            dist/*.tar.gz
            dist/*.zip
